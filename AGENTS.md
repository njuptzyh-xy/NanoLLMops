# NanoLLMOps Development Agents Guide

本文件定义 NanoLLMOps 项目中的默认开发范式。后续所有阶段开发、维护、修复、重构与收尾工作，都应优先遵守本文件。

## 1. 总目标

本项目要求开发过程可追踪、可解释、可回溯。开发不只要求代码完成，也要求任务状态、处理逻辑、改动内容、验证结果和提交记录被持续维护。

## 2. 每一步开发的必做事项

每完成一步明确开发动作后，必须同步完成以下事项：

1. 任务标注
2. 处理逻辑记录
3. 改动内容记录
4. 代码验证
5. Git 提交
6. 推送远程仓库

不能只改代码而不记录，也不能只本地提交而不推送。

## 3. 任务记录规范

每一步开发完成后，必须记录以下内容：

- 当前任务是什么
- 为什么做这一步
- 这一步的大致处理逻辑
- 改动了哪些模块、脚本、文档或测试
- 当前结果是什么
- 是否还有未完成项或后续依赖

推荐优先更新以下文档：

- `docs/开发规划与实时记录.md`
- `docs/phase-xx-*.md`
- `docs/MVP端到端运行说明.md`

如果本次开发是：

- 日常增量开发：至少更新 `docs/开发规划与实时记录.md`
- 阶段性功能完成：除实时记录外，还要更新对应 phase 总结文档
- 运行方式、命令、验证路径发生变化：同步更新运行说明文档

## 4. 文档书写要求

文档必须写明“做了什么”，不能只写“已完成”。

每次记录至少要覆盖：

- 任务名称
- 背景或目的
- 处理步骤或设计逻辑
- 实际改动内容
- 验证方式
- 当前结论
- 下一步

文档内容要求：

- 尽量使用准确文件名、脚本名、模块名
- 尽量写清真实执行结果，而不是计划性描述
- 已完成和未完成要明确分开
- 阶段判断要及时更新，避免文档状态落后于代码状态

## 5. Git 提交规范

每一步开发完成后，必须创建 Git 提交。

要求如下：

- 不允许长期积累大量未提交改动
- 一个提交应对应一组相对完整的开发动作
- 提交前应检查工作区，确认提交内容与本步任务一致
- 提交信息应清楚说明本次改动主题

推荐提交信息风格：

- `Add ...`
- `Update ...`
- `Fix ...`
- `Refactor ...`
- `Document ...`

示例：

- `Add NanoGPT converted bundle validator`
- `Update phase-01 progress and MVP docs`
- `Fix safetensors export for tied weights`

## 6. Git 推送规范

每一步开发完成并提交后，必须推送到远程仓库。

要求如下：

- 默认推送当前工作分支
- 如果当前分支是 `main`，则推送到 `origin main`
- 若推送失败，必须记录失败原因
- 若失败原因是凭据、网络或权限问题，必须在开发结论里明确说明，而不能假装任务已经完全收尾

开发动作只有在以下两种情况之一成立时，才算真正结束：

1. 已成功推送到远程仓库
2. 已明确记录无法推送的真实原因

## 7. 验证规范

每一步开发完成后，应尽可能执行与本次改动直接相关的验证。

验证包括但不限于：

- 单元测试
- 脚本烟测
- 服务健康检查
- 转换产物校验
- 文档命令复核

记录验证结果时应写明：

- 执行了什么命令
- 结果是通过还是失败
- 如果失败，失败原因是什么

## 8. 阶段收尾规范

如果本次开发使项目进入了新的阶段，或者完成了一个阶段内的重要里程碑，还必须补做以下事项：

1. 更新当前阶段判断
2. 更新阶段完成项
3. 更新剩余未完成项
4. 更新下一步执行重点

不能出现代码已经进入下一阶段，而文档仍停留在旧阶段的情况。

## 9. 默认执行顺序

后续开发默认按以下顺序执行：

1. 明确当前任务
2. 实施代码或文档改动
3. 执行必要验证
4. 更新开发记录与相关文档
5. 检查 `git diff` 和 `git status`
6. 创建 Git 提交
7. 推送远程仓库
8. 汇报结果

## 10. 对代理开发的硬性要求

作为本项目中的开发代理，默认应遵守以下约束：

- 不把“代码改完但没写文档”视为完成
- 不把“本地提交了但没推送”视为完成
- 每一步开发结束时，优先检查是否遗漏任务记录和阶段文档更新
- 如果发现当前变更会影响既有开发范式，应先更新本文件或相关规范文档，再继续推进

## 11. 本项目当前明确要求

当前用户已经明确提出以下长期规则，后续开发默认严格执行：

1. 每一步开发完成都应该完成任务标注、任务处理逻辑记录、改动内容文档书写
2. 每一步开发都应该提交 Git，并推送远程仓库

如果未来规范有新增，应继续追加到本文件，而不是只停留在临时对话中。
