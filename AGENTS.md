# NanoLLMOps Development Agents Guide

本文件定义 NanoLLMOps 项目中的默认开发范式。后续所有阶段开发、维护、修复、重构与收尾工作，都应优先遵守本文件。

## 1. 总目标

本项目要求开发过程可追踪、可解释、可回溯。开发不只要求代码完成，也要求任务状态、处理逻辑、改动内容、验证结果和提交记录被持续维护。

## 2. 每一步开发的必做事项

每完成一步明确开发动作后，必须同步完成以下事项：

1. 任务标注
2. 处理逻辑记录
3. 改动内容记录
4. 代码验证
5. Git 提交
6. 推送远程仓库

不能只改代码而不记录，也不能只本地提交而不推送。

## 3. 任务记录规范

每一步开发完成后，必须记录以下内容：

- 当前任务是什么
- 为什么做这一步
- 这一步的大致处理逻辑
- 改动了哪些模块、脚本、文档或测试
- 当前结果是什么
- 是否还有未完成项或后续依赖

开发记录统一写入：

- `docs/logs/TASK-任务编号_YYYY-MM-DD-HHmm_任务名称.md`

如果本次开发是：

- 日常增量开发：更新 `docs/开发规划.md` 中的任务状态，并新增或更新对应任务日志
- 阶段性功能完成：除任务日志外，还要更新对应 phase 总结文档
- 运行方式、命令、验证路径发生变化：同步更新运行说明文档

## 4. 文档书写要求

文档必须写明“做了什么”，不能只写“已完成”。

每次记录至少要覆盖：

- 任务名称
- 背景或目的
- 处理步骤或设计逻辑
- 实际改动内容
- 验证方式
- 当前结论
- 下一步

文档内容要求：

- 尽量使用准确文件名、脚本名、模块名
- 尽量写清真实执行结果，而不是计划性描述
- 已完成和未完成要明确分开
- 阶段判断要及时更新，避免文档状态落后于代码状态

## 5. Git 提交规范

每一步开发完成后，必须创建 Git 提交。

要求如下：

- 不允许长期积累大量未提交改动
- 一个提交应对应一组相对完整的开发动作
- 提交前应检查工作区，确认提交内容与本步任务一致
- 提交信息应清楚说明本次改动主题

推荐提交信息风格：

- `Add ...`
- `Update ...`
- `Fix ...`
- `Refactor ...`
- `Document ...`

示例：

- `Add NanoGPT converted bundle validator`
- `Update phase-01 progress and MVP docs`
- `Fix safetensors export for tied weights`

## 6. Git 推送规范

每一步开发完成并提交后，必须推送到远程仓库。

要求如下：

- 默认推送当前工作分支
- 如果当前分支是 `main`，则推送到 `origin main`
- 若推送失败，必须记录失败原因
- 若失败原因是凭据、网络或权限问题，必须在开发结论里明确说明，而不能假装任务已经完全收尾

开发动作只有在以下两种情况之一成立时，才算真正结束：

1. 已成功推送到远程仓库
2. 已明确记录无法推送的真实原因

## 7. 验证规范

每一步开发完成后，应尽可能执行与本次改动直接相关的验证。

验证包括但不限于：

- 单元测试
- 脚本烟测
- 服务健康检查
- 转换产物校验
- 文档命令复核

记录验证结果时应写明：

- 执行了什么命令
- 结果是通过还是失败
- 如果失败，失败原因是什么

## 8. 阶段收尾规范

如果本次开发使项目进入了新的阶段，或者完成了一个阶段内的重要里程碑，还必须补做以下事项：

1. 更新当前阶段判断
2. 更新阶段完成项
3. 更新剩余未完成项
4. 更新下一步执行重点

不能出现代码已经进入下一阶段，而文档仍停留在旧阶段的情况。

## 9. 默认执行顺序

后续开发默认按以下顺序执行：

1. 阅读 `AGENTS.md`
2. 阅读 `docs/文档索引.md`
3. 阅读产品文档、当前阶段技术文档和运行说明
4. 从 `docs/开发规划.md` 领取一个任务编号
5. 将任务状态更新为 `IN_PROGRESS`
6. 实施代码或文档改动
7. 执行必要验证
8. 在 `docs/logs/` 中更新对应任务日志
9. 将规划中的任务状态、日志链接和后续任务更新到真实状态
10. 检查 `git diff` 和 `git status`
11. 创建 Git 提交
12. 推送远程仓库
13. 汇报结果

## 10. 对代理开发的硬性要求

作为本项目中的开发代理，默认应遵守以下约束：

- 不把“代码改完但没写文档”视为完成
- 不把“本地提交了但没推送”视为完成
- 每一步开发结束时，优先检查是否遗漏规划状态、任务日志和阶段文档更新
- 如果发现当前变更会影响既有开发范式，应先更新本文件或相关规范文档，再继续推进

## 11. 本项目当前明确要求

当前用户已经明确提出以下长期规则，后续开发默认严格执行：

1. 每一步开发完成都应该完成任务标注、任务处理逻辑记录、改动内容文档书写
2. 每一步开发都应该提交 Git，并推送远程仓库
3. 开发日志应优先说明当前阶段的真实状态，避免术语堆叠和过期内容干扰判断
4. 开发规划和开发日志必须分开维护
5. 每个开发日志必须使用与规划任务关联的任务编号，并存放到 `docs/logs/`

如果未来规范有新增，应继续追加到本文件，而不是只停留在临时对话中。

## 12. 开发文档分层规范

开发规划、任务日志和阶段总结承担不同职责，不允许重新混写到同一个文件中。

### 12.1 开发规划职责

`docs/开发规划.md` 是 Codex 领取任务的唯一规划入口。

规划文件只维护：

- 当前阶段
- 任务编号
- 任务名称
- 任务状态
- 交付目标
- 前置依赖
- 对应日志链接

规划文件不允许持续追加详细处理过程、命令输出和 Git 故障流水。

### 12.2 开发日志命名

开发日志统一放在 `docs/logs/`，文件名格式：

```text
TASK-任务编号_YYYY-MM-DD-HHmm_任务名称.md
```

示例：

```text
TASK-P2-001_2026-06-02-2003_nano-vllm原生接入点调研.md
```

同一任务跨多日开发时，按日期新增日志，不覆盖既有日志。

### 12.3 开发日志内容

每个任务日志至少写明：

- 任务编号和任务名称
- 背景或目的
- 处理步骤或设计逻辑
- 实际改动内容
- 验证命令和结果
- 当前结论
- 未完成项或下一步
- Git 提交和推送结果

### 12.4 优先回答当前事实

日志顶部或阶段总结中，应优先写清：

1. 当前处于什么阶段
2. 当前阶段目标是什么
3. 当前修改了什么
4. 修改后达到什么效果
5. 重点确定了什么解决思路
6. 当前阶段的核心代码在哪儿
7. 是否有针对当前阶段的测试脚本
8. 测试脚本在哪儿
9. 测试结果如何
10. 当前仍有哪些明确未完成项

### 12.5 控制术语密度

- 使用直接、可验证的描述，避免连续堆叠术语
- 如果必须使用术语，应同时说明它解决了什么问题
- 优先写文件路径、执行结果和当前结论
- 不使用模糊表述代替真实结果，例如只写“能力增强”“链路优化”“持续推进”

### 12.6 区分规划、日志与阶段总结

- `docs/开发规划.md` 只保留任务定义和状态
- `docs/logs/` 保存按任务编号关联的开发流水
- 阶段总结文档应及时删除或改写已经完成的待办，避免旧结论与当前状态冲突

### 12.7 明确测试状态

每个阶段都应明确说明：

- 已有哪些测试脚本
- 每个测试脚本覆盖什么
- 实际执行了什么验证命令
- 验证结果是通过还是失败
- 当前还缺哪些测试

如果某个核心模块尚无独立测试，必须直接写明，不得省略。

## 13. 开发前文档读取规范

每次开始新任务前，默认按以下顺序读取：

1. `AGENTS.md`
2. `docs/文档索引.md`
3. `nanoLLMops产品文档.md`
4. `docs/开发规划.md`
5. 当前任务关联的 phase 总结、技术文档和运行说明

如果规划中的当前任务不明确，先整理规划文档，再继续开发。
